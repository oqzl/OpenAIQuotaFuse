{
  "name": "openai-quota-fuse-viewer",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash scripts/prepare-deploy.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
